paystub_app/
│ main.py                     ← Flask app with multi-page PDF logic
│ requirements.txt            ← (auto-generate if you want)
│
├── templates/
│     form.html               ← Styled creation form
│     paystub.html            ← Corporate paystub template
│
├── static/
│     (empty, ready for CSS/icons)
│
└── uploads/
      (logo upload storage)
